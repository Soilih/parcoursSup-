{% extends 'base.html.twig' %}

{% block title %}TypeUniversite{% endblock %}

{% block body %}
    <h1>TypeUniversite</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ type_universite.id }}</td>
            </tr>
            <tr>
                <th>Libelle</th>
                <td>{{ type_universite.libelle }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('type_universite_index') }}">back to list</a>

    <a href="{{ path('type_universite_edit', {'id': type_universite.id}) }}">edit</a>

    {{ include('type_universite/_delete_form.html.twig') }}
{% endblock %}
