<form method="post" action="{{ path('parcours_universitaire_delete', {'id': parcours_universitaire.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ parcours_universitaire.id) }}">
    <button class="btn btn-danger btn-sm"><i class="fas fa-trash"></i></button>
</form>
