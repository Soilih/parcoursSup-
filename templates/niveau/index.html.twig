

{% extends 'base.html.twig' %}
{% block body %}
<div class="wrapper">
  <div class="content-wrapper">
     <div class="row" style="padding-left:2%;padding-top:2%">
          <div class="col-md-12">
            <div class="card card-success">
              <div class="card-header">
                <h3 class="card-title" style="font-size:22px">
                 <strong> Gestion niveau  </strong>
               </h3>
                <a  href="{{ path("niveau_new") }}" class="btn btn-primary" style="float:right">Ajouter un niveau  </a>
              </div>
              <!-- /.card-header -->
              <div class="card-body table-responsive p-1">
                <table class="table table-bordered">
                    <thead>
                        <tr>
                         <th>N°</th>
                        <th>libelle</th>
                        <th>Action</th>
                      </tr>
                    </thead>
                    <tbody>
                   {% for niveau in niveaux %}
                  <tr>
                    <td>{{ niveau.id }}</td>
                    <td>{{ niveau.libelle  }}</td>
                    <td>
                    
                    <button class="btn btn-primary btn-xs" style="display-inline:block"> <a href="{{ path('niveau_edit', {'id': niveau.id}) }}"><i style="color:white" class="fa fa-pencil"></i></a></button>
                    <div style="display: inline-block;padding-left:2% ; color:red">{{ include('niveau/_delete_form.html.twig') }} </div> 
                     
                     
                     </td>
                  </tr>
                  {% endfor %}  
                    </tbody>
                </table>
              </div>
              <!-- /.card-body -->
            </div>
          </div>  
        </div>
    </div>
</div>
{% endblock %}

